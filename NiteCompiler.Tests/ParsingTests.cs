using static TUnit.Assertions.Assert;

namespace NiteCompiler.Tests;

public class ParsingTests
{
    [Test]
    public async Task MyTest() // TODO: Fulfill with a lot of parser+lexer tests
    {
        await That(true).IsTrue();
    }
}